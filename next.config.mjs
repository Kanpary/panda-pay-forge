/** @type {import('next').NextConfig} */
const nextConfig = {
  allowedDevOrigins: [
    'painel-administrativo-integrado-35.v0.build',
    'sb-h10aetkv11hw.vercel.run',
  ],
}

export default nextConfig
